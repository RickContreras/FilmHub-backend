# Configuración del puerto HTTP
server.port=8080

# Database
spring.datasource.url=jdbc:postgresql://localhost:5432/filmhub
spring.datasource.username=postgres
spring.datasource.password=root1
spring.datasource.driver-class-name=org.postgresql.Driver

# Configuraci�n de JPA/Hibernate

# Define el dialecto de SQL que Hibernate debe usar para PostgreSQL.
# Esto asegura que las consultas generadas sean compatibles con PostgreSQL.
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Controla el comportamiento de Hibernate con respecto al esquema de la base de datos.
# - 'create' elimina y vuelve a crear las tablas cada vez que la aplicaci�n se inicia.
# - 'update' actualiza el esquema para que coincida con las entidades, sin eliminar columnas existentes.
# - 'create-drop' crea las tablas y las elimina al detenerse la aplicaci�n.
# - 'validate' valida que el esquema coincida con las entidades sin hacer cambios.
# - 'none' no realiza ninguna acci�n sobre el esquema.
spring.jpa.hibernate.ddl-auto=create-drop

#Nota: Para un entorno de desarrollo, esto puede ser �til, pero para producci�n, se recomienda usar herramientas de migraci�n como Flyway o Liquibase para mayor control.

# Controla si Hibernate debe mostrar las consultas SQL generadas en la consola.
# Esto es �til para la depuraci�n y para ver qu� consultas est� ejecutando Hibernate.
spring.jpa.show-sql=true

# Indica si Hibernate debe formatear las consultas SQL generadas para que sean m�s legibles.
# Esto facilita la lectura y depuraci�n de las consultas en la consola.
 spring.jpa.properties.hibernate.format_sql=true


 # Configuración del puerto HTTPS
 # server.ssl.enabled=true
 # server.port=8443
 # server.ssl.key-store=classpath:keystore.p12
 # server.ssl.key-store-password=prueba
 # server.ssl.keyStoreType=PKCS12
 # server.ssl.keyAlias=tomcat
 # el archivo keystore.p12 se encuentra en la carpeta resources a nivel de este archivo
 # Se puede generarar con el siguiente comando:
 # keytool -genkeypair -alias tomcat -keyalg RSA -keysize 2048 -storetype PKCS12 -keystore keystore.p12 -validity 3650